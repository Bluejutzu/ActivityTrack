{
  "name": "@activitytrack/api",
  "version": "0.1.0",
  "private": true,
  "description": "ActivityTrack standalone ingress API — Bun + Elysia. Handles agent pairing, heartbeats, Genesys relay, and Clockodo webhooks.",
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "start": "bun src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@activitytrack/backend": "workspace:*",
    "@activitytrack/shared": "workspace:*",
    "convex": "^1.16.0",
    "elysia": "^1.4.28"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  }
}
