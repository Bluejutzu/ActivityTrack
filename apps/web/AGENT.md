# Deprecated

`apps/web` is deprecated. Do not add new features or spend significant
effort here — treat any work in this directory as maintenance-only
(critical fixes) unless a human explicitly says otherwise.
